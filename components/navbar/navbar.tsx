"use client";

const Navbar = () => {
  return (
    <nav className="w-full px-4 py-2 flex items-center shadow-md sticky top-0 bg-primary-light dark:bg-primary-dark">
      <div>
        <h1 className="font-quicksand tracking-wider text-primary-dark dark:text-primary-light text-4xl">
          Confessions
        </h1>
      </div>

      <div>
        
      </div>
    </nav>
  );
};

export default Navbar;
